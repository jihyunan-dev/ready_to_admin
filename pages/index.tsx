import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
    return (
        <>
            <Head>
                <title>ADMIN TEST</title>
            </Head>
            <div>HOME!!</div>
        </>
    );
};

export default Home;
